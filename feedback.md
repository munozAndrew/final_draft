## Professor Feedback

### Formatting and readability

> "The overall formatting needs work. Use paragraphs and sections to make it easier to read and scan. This may just be markdown eating your formatting."

The structure is: Background → Hypothesis → Data → Results → Discussion → Summary. Every section starts with a paragraph of pros before any bullet lists or tables.

### Background on prediction markets

> "The narrative assumes too much background in prediction markets. What is Kalshi for instance? Is it a different market than Polymarket? Or is that the wrong way to think of it?"

A **Background** section was added at the top of the writeup. It explains what prediction markets are, describes Kalshi specifically (U.S. regulated CFTC licensed exchange, binary contracts, etc), and then explains Polymarket (decentralized, Polygon blockchain, USDC). The section clarifies that both are prediction markets but are distinct platforms with different structures.

### Missing visualizations

> "Your narrative needs some visualizations. You refer to figures, but they don't exist in your writeup."

All figures referenced in the writeup are generated by running `final_analysis.py` and are saved to `output_final/`.

## Peer Reviewer Feedback

### Issue 1: Analysis stays largely descriptive

> "Moving beyond this style and more towards incorporating a more formal tone would strengthen the overall argument."

Two additions were made:

1. A full multivariate OLS coefficient table was added to the writeup in the **Controlling for Domain** results section. It shows coefficients, standard errors, t-statistics, and p-values directly.

2. The same table is generated by `final_analysis.py` and saved to `output_final/ols_summary.txt`. This makes the results clearly visible instead of only appearing in figure annotations.

### Issue 2: Causality vs. correlation

> "Throughout your report, it is implied that longer questions *cause* worse accuracy. Your analysis is purely observational."

Language was changed throughout the writeup. "Longer questions are priced less accurately" was replaced with "longer questions are *associated* with worse accuracy." The Limitations section explicitly states: "Causality is not established. This is just observational analysis.

### Issue 3: Word count immediately implies complexity

> "A 'short' question may still be ambiguous and technically dense."

The Hypothesis section now explicitly states that word count is used as a *proxy* for complexity and acknowledges that it is not a perfect measure. The Limitations section reinforces this, The original framing of "longer = more complex" was changed to "word count as a proxy for complexity."

### Issue 4: No control variables

> "A multivariate regression that takes into account other factors (i.e., domain) would be helpful. I would check LINE assumptions to see if this model can be used, and also Cook's distance/residuals."

Three additions were made in `final_analysis.py`:

**Multivariate OLS:** The `multivariate_ols_np()` function runs OLS with word count and domain dummies as predictors The `word_count` coefficient is β = 0.00111 (p < 0.001) even after controlling for domain, confirming the association is not entirely explained by domain membership. Results are printed to stdout and saved to `output_final/ols_summary.txt`.

**LINE diagnostics (Fig 4):** `fig4_ols_diagnostics()` produces a 2×2 panel: residuals vs fitted, QQ plot, scale-location, and residual histogram. The diagnostics show what is expected given the data. Brier scores are bounded at 0 and right-skewed, so residuals are non-normal and variance is heteroscedastic. This is discussed directly in the writeup. With n = 114,127, the t-statistics are reliable by the CLT despite non-normal residuals.

**Cook's distance (Fig 5):** `fig5_category_diagnostics()` computes Cook's distance and standardized residuals for the category-level bivariate OLS. The individual market model has over 100,000 observations where Cook's distance would be negligible for all points, so this diagnostic is more meaningful at the category level where the sample size is only dozens of rows.

> "To further validate your conclusion a p-value and slope would be good to show the strength of association."

The slope (β = 0.0024 per word) and p-value (p = 0.087) for the category level OLS in Figure 3 are now labeled explicitly in the legend. The multivariate model slope (β = 0.00111, p < 0.001) is shown in the results table. The writeup is clear that slope is not significant at α = 0.05.

> "Entertainment/Awards has the shortest average word length (11 words) but the second largest mean brier score. In the writeup you state it has an average of 12.8 words. Could be an old graph?"

The figure from the draft run showed 11 words for E/A, but the text said 12.8. The text was using a stale number from an earlier run. The new `final_analysis.py` computes both figures and text numbers from the same run, so they are now consistent. E/A's actual average is **11.0 words** (Brier ≈ 0.042), and the writeup now uses this number. The E/A anomaly (short questions, bad accuracy) is now discussed explicitly as a domain level exception to the hypothesis.

> "Looking at the sample size for each word count group, n should be equal if these are quintiles, but n varies a lot between groups."

This is explained in the Results section and in a code comment in `brier_by_bucket()`. `pd.qcut` targets equal frequency bins, but when many markets share the same word count (ties at a percentile boundary), it has to merge bins to avoid creating empty groups. The result is unequal n per bucket. This is expected behavior, not a data error. The figure labels now show the actual word count range and n for each bucket so readers can see this directly.

---
